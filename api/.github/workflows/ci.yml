name: CI

on: 
  push: 
    branches: 
      - main

permissions:
  id-token: write
  contents: read

jobs:
  build:
    name: "Build and push"
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v5
      
      - name: Setup node
        uses: actions/setup-node@v5
        with: 
          node-version: 22
          cache: npm
      
      - run: npm i
      - run: npm run test

      - name: Generate tag
        id: generate_tag
        run: |
          SHA=$(echo $GITHUB_SHA | head -c7)
          echo "sha=$SHA" >> $GITHUB_OUTPUT

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v6.2.3
        with: 
          aws-region: us-east-2
          role-to-assume: 'arn:aws:iam::831181104004:role/ecr_role'

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v1


      # - name: Login into the container registry
      #   uses: docker/login-action@v4
      #   with:
      #     username: ${{secrets.DOCKERHUB_USERNAME}}
      #     password: ${{secrets.DOCKERHUB_TOKEN}}

      # - name: Build and push
      #   uses: docker/build-push-action@v7
      #   with:
      #     push: true
      #     context: .
      #     tags: 
      #       ${{secrets.DOCKERHUB_USERNAME}}/ci-cd-estudos:${{steps.generate_tag.outputs.sha}}, 
      #       ${{secrets.DOCKERHUB_USERNAME}}/ci-cd-estudos:latest

      # - name: Build docker image
      #   run: docker build -t ${{secrets.DOCKERHUB_USERNAME}}/ci-cd-estudos:${{steps.generate_tag.outputs.sha}} .

      # - name: Push Image
      #   run: docker push ${{secrets.DOCKERHUB_USERNAME}}/ci-cd-estudos:${{steps.generate_tag.outputs.sha}}
